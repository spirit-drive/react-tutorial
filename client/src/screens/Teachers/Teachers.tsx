import React, { FC } from 'react';
import { Page } from 'src/components/Page';
import s from './Teachers.sass';

export const Teachers: FC = () => (
  <Page title="Teachers" className={s.root}>
    Teachers
  </Page>
);

export default Teachers;
