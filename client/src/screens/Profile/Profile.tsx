import React, { FC } from 'react';
import s from './Profile.sass';

export const Profile: FC = () => <div className={s.root}>Никакого секрета, просто защищенный роутинг</div>;

export default Profile;
